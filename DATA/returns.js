//that takes one argument and return it
function id (arg) {
    return arg
}
//that takes an array or a string and return its length
function getLength (arr) {
    return arr.length // retourne la length de la var 
}